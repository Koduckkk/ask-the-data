# Database schema

Auto-generated by `src/load.py` from the loaded DuckDB tables. Do not
edit by hand — it is regenerated on every load and is the grounding the
NL→SQL layer reads to write queries against real column names.

## `students`  (50,000 rows)

| column | type | description |
|---|---|---|
| `student_id` | VARCHAR | canonical student identifier (normalised across sources) |
| `enrolment_id` | VARCHAR | local enrolment id (leading zeros normalised) |
| `year_level` | BIGINT | year level: 3, 5, 7 or 9 |
| `school_id` | VARCHAR | school identifier (ACARA id) |
| `family_name` | VARCHAR | family name (encoding repaired, junk stripped) |
| `given_name` | VARCHAR | given name |
| `middle_name` | VARCHAR | middle name (may be empty) |
| `gender` | VARCHAR | gender: M, F or X |
| `birth_date` | VARCHAR | date of birth (ISO yyyy-mm-dd) |

## `results`  (250,000 rows)

| column | type | description |
|---|---|---|
| `student_id` | VARCHAR | canonical student identifier (normalised across sources) |
| `year_level` | BIGINT | year level: 3, 5, 7 or 9 |
| `school_id` | VARCHAR | school identifier (ACARA id) |
| `domain` | VARCHAR | assessment domain: Reading, Numeracy, Spelling, Grammar and Punctuation, Writing |
| `raw_score` | DOUBLE | raw score (sum of item marks); null if not participated |
| `scaled_score` | DOUBLE | scaled score; refused students overridden to 0 |
| `proficiency` | VARCHAR | proficiency band |
| `test_year` | BIGINT | calendar year of the test |

## `schools`  (40 rows)

| column | type | description |
|---|---|---|
| `school_id` | VARCHAR | school identifier (ACARA id) |
| `school_name` | VARCHAR | school name |
| `sector` | VARCHAR | school sector: GOV, CATH or IND |
| `postcode` | VARCHAR | school postcode (four digits) |
